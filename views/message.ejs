<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Test</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
</head>

<body>
    <div class="container">
        <% if(isChecked == true){ %>
        <p>Hello
            <%= firstName %>
            <%= lastName %>, Thank you for signing up.
            Your account is now created. You would be receiving our periodic newsletters
            to your email:
            <%= emailId %>
        </p>
        <% } %>
        <% if(isChecked == false){ %>
        <p>Hello
            <%= firstName %>
            <%= lastName %>, Thank you for signing up. Your account
            is now created.</p>
        <% } %>
    </div>
</body>

</html>